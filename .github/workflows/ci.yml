name: CI

# Runs the real test suite on every push and PR across all supported Python
# versions. Complements publish.yml (which only builds + publishes on tags).

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install package + dev deps
        run: python -m pip install -e .[dev]
      - name: Run tests
        run: python -m pytest -q
